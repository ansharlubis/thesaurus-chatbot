## Prerequisites

Set CORS settings following this [reference](https://objectgraph.com/blog/ollama-cors/).

## Usage

1. Run ollama and download model. The default model is `smollm:360m`.

2. Build code

```bash
npm run build
```

3. Open `index.html`